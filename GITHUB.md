# GitHub — push / sync

**Remote:** `origin` → [qbitOS/qbitos-kbatch](https://github.com/qbitOS/qbitos-kbatch)

## Push (after local commits)

```bash
cd /Volumes/qbitOS/00.dev/qbitos-kbatch
git status
git push -u origin main
```

If the GitHub repo is **empty**, the first push establishes `main`.

## Update from uvspeed

See **UPSTREAM.md**, then commit and `git push`.
